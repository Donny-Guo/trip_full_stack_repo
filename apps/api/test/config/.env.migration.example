# Copy to .env.migration.local and replace only the password placeholder with
# the URL-encoded migrator password from .env.compose.local.
NODE_ENV=test
API_MIGRATION_DATABASE_URL=postgresql://trip_migrator:<percent-encoded-migrator-password>@127.0.0.1:55432/trip_auth_api_test
