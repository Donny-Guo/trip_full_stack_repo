# Copy to .env.migration.local, replace the password placeholder, and chmod 600
# the populated ignored file. Never place runtime/provisioner credentials here.
NODE_ENV=development
API_MIGRATION_DATABASE_URL=postgresql://trip_migrator:<percent-encoded-migrator-password>@127.0.0.1:5432/trip
